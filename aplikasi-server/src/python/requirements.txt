# Core dependencies for METEOR evaluation
nltk>=3.8
deep-translator>=1.11.4
langdetect>=1.0.9

# Sentence-BERT dependencies - pinned to lighter versions for Railway compatibility
# PyTorch CPU-only version (lighter, no CUDA)
torch==2.1.0
# Sentence-transformers compatible version
sentence-transformers==2.7.0
# NumPy pinned to 1.26.4 (compatible with torch 2.1.0, avoids libstdc++ issues)
numpy==1.26.4

# ML utilities
scikit-learn>=1.3.0

# Testing dependencies (optional)
hypothesis>=6.0.0
pytest>=7.0.0
