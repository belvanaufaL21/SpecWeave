[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["cd aplikasi-klien && npm ci"]

[phases.build]
cmds = ["cd aplikasi-klien && npm run build"]

[start]
cmd = "cd aplikasi-klien && npm start"

[variables]
NODE_ENV = "production"
