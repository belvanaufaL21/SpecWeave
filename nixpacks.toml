[phases.setup]
nixPkgs = ["nodejs_18", "python39", "python39Packages.pip"]

[phases.install]
cmds = [
  "npm run install:server",
  "cd aplikasi-server && pip install -r src/python/requirements.txt"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "npm start"
