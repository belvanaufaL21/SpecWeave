{
  "name": "specweave-monorepo",
  "version": "1.0.0",
  "type": "module",
  "description": "SpecWeave - Convert User Stories to Gherkin",
  "private": true,
  "scripts": {
    "start": "cd aplikasi-server && npm start",
    "dev": "cd aplikasi-server && npm run dev",
    "install:server": "cd aplikasi-server && npm install",
    "install:client": "cd aplikasi-klien && npm install"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
